{
  "name": "directus-fly",
  "version": "1.0.0",
  "description": "Directus on fly.io starter",
  "scripts": {
    "deploy": "fly deploy --remote-only",
    "start": "npx directus start",
    "dev": "nodemon --exec npx directus start"
  },
  "dependencies": {
    "directus": "^9.21.2",
    "directus-extension-wpslug-interface": "^1.1.0",
    "pdfmake": "^0.2.7",
    "sqlite3": "^5.1.6",
    "tedious": "^16.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
