"use strict";var e=require("fs");require("dotenv").config();var t=t=>{const n=process.env.STORAGE_LOCAL_ROOT||"uploads";t.get("/",((t,o)=>{const s=`${n}/${decodeURIComponent(t.query.docname)}`;if(console.log(`Fetching document from path=${s}`),!e.existsSync(s))return void o.status(404).send("PDF file not found");o.setHeader("Content-Type","application/pdf"),o.setHeader("Content-Disposition","inline; filename=sample.pdf");e.createReadStream(s).pipe(o)}))};module.exports=t;
