export enum FORM_TYPES {
    ASSAY = 'assay',
    CHARGE = 'charge',
    PENALTY = 'penalty',
}

export enum FOR_EVERY_UNIT_TYPE {
    ABOVE = 'above the lower threshold, plus',
    BELOW = 'below the upper threshold, minus',
}

export enum BRACKET_TYPE {
    BRACKET = 'Bracket',
    MAX_CAP = 'Maximum Cap',
    MIN_DEDUCTION = 'Minimum Deduction',
}

export enum RATE_TYPE {
    PERCENTAGE = 'Percentage',
    FRACTIONAL = 'Fractional',
}